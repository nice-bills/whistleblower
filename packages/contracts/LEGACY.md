# Legacy package (not the bounty deliverable)

This Hardhat package is an earlier **Builder Track** whistleblower FHEVM experiment.

The Season 3 **Bounty Track** submission is the registry web app in `packages/web/`.

Run contract tests from the repo root with `pnpm test` if you need to validate this package.
